#include <iostream>
#include <ostream>
using namespace std;

void bubble(void* array, int n, int elem_size, int(*comparator)(const void*)){

}

int main()
{
    // int p[2];
    // int *p = (int*)malloc(sizeof(int)*2);
    // *(p+2) = 1;
    // p[2] = 1;
    return 0;
}